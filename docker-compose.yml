services:
  recorder:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATA_DIR: /app/data
      MAX_UPLOAD_MB: 512
    volumes:
      - ./data:/app/data
    restart: unless-stopped
